import { createContext } from 'react';

/**
 * AuthContext — Context object for Metascale Identity Vectors.
 * Separated to satisfy React-Refresh build rules.
 */
export const AuthContext = createContext();
